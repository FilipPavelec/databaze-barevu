name: Build Windows EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller ttkbootstrap matplotlib
    
    - name: Build EXE
      run: |
        cd python_project
        pyinstaller --onefile --windowed --name="DatabazeBarevu" --add-data="testExport.XML;." --icon=NONE --clean gui.py
    
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: DatabazeBarevu-Windows
        path: python_project/dist/DatabazeBarevu.exe
